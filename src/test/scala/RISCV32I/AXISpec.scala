package RISCV32I

import chisel3._
import chisel3.tester._
import org.scalatest._

/* class AXISpec extends FreeSpec with ChiselScalatestTester {
    "AXI functionality" in {
        test(new AXIL_SLAVE()) { dut =>
            

        }
    }
} */